version=${project.version}
buildNumber=${buildNumber}

# Enabling these properties turns on logging of SQL statements generated by
# Spring Data JPA. These logs are generated by the SqlStatementLogger class
# and occur at DEBUG level, so make sure the root logger is set to DEBUG.
#
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true